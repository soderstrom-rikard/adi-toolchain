# Testing bfin-linux-uclibc toolchain via rcp/gdbserver on a Linux system

load_generic_config "bfin-fmt-fdpic"
load_generic_config "bfin-target-linux"

# Advertise slightly less than we actually have since a few testcases
# really push the limit.
set_board_info gcc,stack_size 0x1f000
